# MH3U NEX server deps. Python 3.13 verified.
# NintendoClients is the MH3U-patched fork (Matt-Wood-23/NintendoClients @ mh3u-revival):
# cloned to ../external/NintendoClients, added to sys.path by server.py — not a pip package.
# (Upstream lacks the legacy-PRUDP-v1 patch MH3U needs and breaks on Python 3.13.)
#
# netifaces stub (FIRST): anynet hard-imports netifaces, but the real one is a C extension
# with NO wheels for Python 3.10+ — so plain pip would compile it from source and fail for
# any host without MSVC build tools. The server never uses netifaces, so we install a tiny
# pure-Python stub that satisfies anynet's import. Result: no compiler needed. Run pip from
# the repo root (the path is relative to it).
./vendor/netifaces_stub
anynet==1.1.0     # PIN: 1.2.x breaks on Py3.13 (forward-ref NameError in tls.py)
anyio
aioconsole
pycryptodome
